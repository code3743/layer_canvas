# Run with `dart run ffigen --config ffigen.yaml`.
name: LayerCanvasBindings
description: |
  Bindings for `src/layer_canvas.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/layer_canvas_bindings_generated.dart'
headers:
  entry-points:
    - 'src/engine.h'
    - 'src/scene_desc.h'
  include-directives:
    - 'src/engine.h'
    - 'src/scene_desc.h'
ffi-native:
comments:
  style: any
  length: full
